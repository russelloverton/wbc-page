services:
  wbc-bracket:
    build: .
    container_name: wbc-bracket
    restart: unless-stopped
    ports:
      - "7452:7452"
    environment:
      - PORT=7452
    volumes:
      - ./data.json:/usr/src/app/data.json
    networks:
      - proxy-nw

networks:
  proxy-nw:
    external: true
